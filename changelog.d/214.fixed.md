- `sync_required_checks` now names a job that a reusable workflow runs `<caller> / <callee>`, as GitHub does. It also skips workflows that do not run on `pull_request`, `pull_request_target`, or `merge_group`. Before this fix, a ruleset synced from the set waited for contexts that never reported.
