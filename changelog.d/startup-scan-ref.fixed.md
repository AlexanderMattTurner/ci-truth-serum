- `startup-failure-scan` now names the ref each jobless failed run used, and it links the newest run. A workflow file loads per ref. A file that is healthy on the default branch still breaks on a branch that edits it. The old report named the file alone, so the reader opened it on the default branch and found that it loads. This repo's own weekly scan went red on three Mondays over 37 runs from one branch, `template-sync`, and no report said so.
