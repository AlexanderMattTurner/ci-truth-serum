- Six review findings on the ported checks, all fixed. `check-curl-retry` and `check-sparse-checkout-closure` now catch `PathologicalInputError` per file instead of crashing the whole hook on a script with more than 2000 piped bytes. `check-wall-clock-assertions`' JavaScript arm resolves a duration name per function, matching its Python arm, so one test's `elapsed` no longer marks an unrelated test's same-named local a duration too. `remerge-diff-report.py`'s blob read now names its repository like every other git call in that file. `run_tier.main` raises `SystemExit` instead of returning an int, matching this pack's own style rule.
