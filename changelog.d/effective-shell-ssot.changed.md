- `_cts_linecheck` now owns three helpers two workflow lints had each encoded on their own. `default_run_shell` reads GitHub's `defaults.run.shell` precedence, which `check-workflow-pipefail` and `check-runner-var-foreign-shell` both need. `step_span_ends` and `container_block_end` say where a step's opt-out may be written, bounded by the step's own job. `check-failure-only-diagnostics` keeps its behaviour and drops its private copy.
