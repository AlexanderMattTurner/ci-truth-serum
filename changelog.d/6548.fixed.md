- `check-path-gate-deps` no longer reads a decide call that passes `derive-paths-regex: true` as a fail-open gap. That input asks the decide job to widen the committed value with the execution closure of the jobs it gates when it runs, so the committed value is a seed and a static read of it answers nothing.
