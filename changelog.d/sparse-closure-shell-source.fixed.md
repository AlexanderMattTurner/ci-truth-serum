- `check-sparse-checkout-closure` now follows a shell entry point's own `source` lines, as it already followed a Python entry point's imports. A list that names the script and not the library it sources leaves the runner without that library, and the step dies with `No such file or directory` the first time that code path runs. The check reads the `# shellcheck source=` directive the script carries, a literal relative path, and — for the `source "$(dirname "${BASH_SOURCE[0]}")/lib.sh"` idiom no static reader can compute — the last segment resolved against the script's own directory. Every candidate must name a tracked file, so a path an expansion decides adds nothing rather than a hole no list can close. Measured against a live failure: a job whose list named `auto-approve-skipped-pr.sh` alone.
