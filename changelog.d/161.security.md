- ci-truth-serum now pins every dependency to an exact version instead of a floor. The pack installs into a pre-commit venv or a CI job of its own. A floor there served no consumer's resolution. It only let each install take whatever the index served that day. The pack pins its build backend (`setuptools`) for the same reason. Bump a pin and run `uv lock` in the same commit.
