- Dependencies are pinned to exact versions instead of floors. This package is a lint pack: it installs into a pre-commit venv or a CI job of its own, so a floor never served a consumer's resolution — it only let each install take whatever the index served that day. The build backend (`setuptools`) is pinned for the same reason. Bump a pin and run `uv lock` in the same commit.
