- `UnparseableShellError` now names a byte offset and one remedy. It already named a line number. Fix the syntax. The known triggers and their behaviour-identical rewrites are in the class docstring. Another option is to add the file to the shell hook's `exclude:` pattern in `.pre-commit-config.yaml`. Use this option when the file is not real bash. Before this change, a pre-commit run named the broken line only.
